{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-read --unstable Bot/app.ts",
      "desc": "run my app.ts file"
    }
  }
}
